// const newArr = (arr) => {
//     return `В массиве ${arr.length} элементов`;
// };

// const sum = (x, y) => {
//     return `Число ${x} плюс число ${y} равно ${x + y}`;
// };

// const num = 345;


// module.exports.newArr = newArr;
// module.exports.sum = sum;
// module.exports.num = num;

// =============================

// const newArr = (arr) => {
//     return `В массиве ${arr.length} элементов`;
// };

// const sum = (x, y) => {
//     return `Число ${x} плюс число ${y} равно ${x + y}`;
// };

// const num = 34;


// module.exports = {
//     newArr: newArr,
//     sum: sum,
//     num: num
// };

// ==============================

module.exports.newArr = (arr) => {
    return `В массиве ${arr.length} элементов`;
};

module.exports.sum = (x, y) => {
    return `Число ${x} плюс число ${y} равно ${x + y}`;
};

module.exports.num = 22;

